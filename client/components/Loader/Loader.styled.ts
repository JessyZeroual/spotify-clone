import styled from "styled-components/native";
import { Container } from "../../styles/commonStyled";

const WrapperLoader = styled(Container)`
  display: flex;
  justify-content: center;
  align-items: center;
`;

export default WrapperLoader;
